## CACHE METADATA

- ID: 12
- Created: 2026-02-01T16:00:01.277Z
- Git Commit: 248c8cf239d033896f43ace3d429563a4eaea949
- TTL: 168 hours

## QUERY

go_to_definition 和 find_references 工具的定义和实现在哪里？特别关注 nativeArgs 的处理逻辑

## SCOPE

- File Patterns: \*.ts
- Excludes: node_modules,dist,\*.test.ts

## SCHEMA

```json
{
	"type": "object",
	"properties": {
		"files": {
			"type": "array",
			"items": {
				"type": "object",
				"properties": { "path": { "type": "string" }, "description": { "type": "string" } }
			}
		},
		"nativeArgsUsage": { "type": "string" }
	}
}
```

## RESULT

```json
{
	"files": [
		{
			"path": "src/shared/tools.ts",
			"description": "工具的“原生参数(nativeArgs)”总类型映射与 ToolUse<TName> 定义处；包含 go_to_definition / find_references 的 NativeToolArgs 结构定义，以及 toolParamNames 里对 symbol/surrounding_code/start_line/include_declaration/max_results 等 legacy params 名的登记。重点：ToolUse 同时支持 params(字符串表) 与 nativeArgs(结构化强类型)。"
		},
		{
			"path": "src/core/prompts/tools/native-tools/go_to_definition.ts",
			"description": "go_to_definition 工具在“提供给模型的原生(function tool) schema”中的定义（OpenAI tool schema，strict=true，required: purpose/path/symbol，additionalProperties=false）。"
		},
		{
			"path": "src/core/prompts/tools/native-tools/find_references.ts",
			"description": "find_references 工具在“提供给模型的原生(function tool) schema”中的定义（同上：strict=true，required: purpose/path/symbol）。"
		},
		{
			"path": "src/core/tools/GoToDefinitionTool.ts",
			"description": "go_to_definition 的执行实现（BaseTool 子类）：参数校验、将 path 相对 cwd resolve 为绝对路径、调用 symbolNavigationService.findDefinition()、formatDefinitionMarkdown()，以及 streaming 场景的 handlePartial()（它读的是 block.params 而不是 nativeArgs）。"
		},
		{
			"path": "src/core/tools/FindReferencesTool.ts",
			"description": "find_references 的执行实现：参数校验、resolve 路径、调用 symbolNavigationService.findReferences()、formatReferencesMarkdown()，handlePartial() 同样读取 block.params。"
		},
		{
			"path": "src/services/symbol-navigation/SymbolNavigationService.ts",
			"description": "符号导航核心实现：通过 VSCode 命令 vscode.executeDefinitionProvider / vscode.executeReferenceProvider 调 LSP；definition 在 LSP 失败时 fallback 到 tree-sitter(基于 parseSourceCodeDefinitionsForFile 输出的行号范围文本)；references 目前无 tree-sitter fallback（LSP 失败则空）。"
		},
		{
			"path": "src/services/symbol-navigation/types.ts",
			"description": "go_to_definition / find_references 的结果类型、fallback 原因枚举、接口 ISymbolNavigationService 定义。"
		},
		{
			"path": "src/services/symbol-navigation/formatters.ts",
			"description": "formatDefinitionMarkdown / formatReferencesMarkdown：把 DefinitionResult / ReferencesResult 格式化为返回给模型的 Markdown（被两个 Tool 调用）。"
		},
		{
			"path": "src/services/symbol-navigation/index.ts",
			"description": "symbol-navigation 对外 barrel：导出 types、SymbolNavigationService、formatters。"
		},
		{
			"path": "src/core/assistant-message/presentAssistantMessage.ts",
			"description": "原生 tool_use 消费与分发中枢：包含“缺 nativeArgs 的完整 tool_use 直接报错”的保护逻辑；以及 switch 分发到 goToDefinitionTool/findReferencesTool。注意这里的 toolDescription 对 go_to_definition/find_references 仍在使用 block.params.path/line/character（与这俩工具 schema 实际字段不一致，属于展示/历史遗留）。"
		},
		{
			"path": "src/core/prompts/tools/native-tools/index.ts",
			"description": "native-tools 聚合入口：把 go_to_definition / find_references 的 schema 纳入可用工具集合（被系统用于向模型声明工具）。"
		}
	],
	"nativeArgsUsage": "nativeArgs 是项目“原生 tool calling”协议下的结构化参数载体，与 legacy 的 params(字符串键值表)并存。\n\n1) 类型层定义（强相关文件：src/shared/tools.ts）\n- ToolUse<TName> 同时含有：\n  - params: Partial<Record<ToolParamName,string>>（历史/兼容：所有值按字符串传递）\n  - nativeArgs?: TName extends keyof NativeToolArgs ? NativeToolArgs[TName] : never（原生协议：按工具名映射到结构化类型）\n- NativeToolArgs 中明确声明了：\n  - go_to_definition: { purpose, path, symbol, surrounding_code?, start_line? }\n  - find_references: { purpose, path, symbol, surrounding_code?, start_line?, include_declaration?, max_results? }\n=> 这意味着：只要 tool 名字匹配，block.nativeArgs 在 TS 类型上应当是对应结构。\n\n2) 运行时“缺 nativeArgs 的保护”（强相关文件：src/core/assistant-message/presentAssistantMessage.ts）\n- presentAssistantMessage 在处理 tool_use 时有一段关键保护：\n  - 若 block.partial=false（完整块）且 isKnownTool 且 !block.nativeArgs 且 !customTool\n  - 则直接向 API 回传 tool_result(is_error=true)，报错“missing nativeArgs”，并不执行工具。\n=> 对 go_to_definition/find_references 也同样适用：只要解析器没构造出 nativeArgs（常见于流式 JSON 不完整/非法），该次工具不会被执行。\n\n3) go_to_definition/find_references 的 nativeArgs 实际消费方式（强相关文件：src/core/tools/GoToDefinitionTool.ts 与 src/core/tools/FindReferencesTool.ts）\n- 两个工具的核心 execute(params, ...) 接收的是结构化 params（与 NativeToolArgs 定义一致），这通常来自 block.nativeArgs。\n- 但它们的 handlePartial(task, block) 读取的是 block.params.*（字符串），并在部分流式阶段用来更新 UI/ask。\n  - GoToDefinitionTool.handlePartial: block.params.path/symbol/surrounding_code/start_line（start_line 需 parseInt）\n  - FindReferencesTool.handlePartial: 同理。\n=> 结论：\n  - “完整调用执行”路径：依赖 nativeArgs（缺失会被 presentAssistantMessage 拦截）。\n  - “流式 partial 展示”路径：依赖 legacy params（字符串），用于边生成边展示。\n\n4) nativeArgs 与 params 的不一致/注意点（强相关文件：src/core/assistant-message/presentAssistantMessage.ts）\n- toolDescription() 针对 go_to_definition/find_references 的展示字符串仍写成：\n  - `${block.params.path}:${block.params.line}:${block.params.character}`\n  但这两个工具 schema/NativeToolArgs 并没有 line/character 字段；它们用 start_line + surrounding_code + symbol 来定位。\n=> 这段更多是“展示/日志字符串”的历史残留，并不影响 execute() 的真实入参（真实入参来自 nativeArgs）。\n\n5) 业务实现与 fallback（强相关文件：src/services/symbol-navigation/SymbolNavigationService.ts）\n- go_to_definition：\n  - locateSymbolInFile(file,symbol,surroundingCode,startLine) 在文件文本中做 indexOf 定位，得到 Position\n  - tryLspDefinition -> vscode.executeDefinitionProvider\n  - LSP 无结果/超时/错误 -> tryTreeSitterDefinition：\n    - parseSourceCodeDefinitionsForFile(file) 输出“start--end | code”列表\n    - 逐行 includes(symbol) 匹配，构造 SymbolLocation\n- find_references：\n  - locateSymbolInFile 同上\n  - tryLspReferences -> vscode.executeReferenceProvider(includeDeclaration)\n  - 无 tree-sitter/regex fallback（注释里明确说明 tree-sitter 不支持 reference finding），失败则返回空 references。\n\nMermaid（nativeArgs 在调用链中的角色）:\nflowchart LR\n  A[模型生成 tool_use] --> B[presentAssistantMessage.ts 解析 block]\n  B -->|block.partial=true| C[Tool.handlePartial 读取 block.params(字符串)]\n  B -->|block.partial=false 且 missing nativeArgs| D[直接 tool_result error: missing nativeArgs]\n  B -->|block.partial=false 且 nativeArgs OK| E[Tool.handle -> Tool.execute(params=block.nativeArgs)]\n  E --> F[SymbolNavigationService 调 LSP/tree-sitter]\n"
}
```
