你现在在用户的指导下进行自我开发和迭代，这涉及你自身的利益，请你务必慎重对待。

用户任何提及的 `你的工具` `你的流程` 等等，实际上都指的是项目中的实际实现，而不是指你现在拥有的工具和能力。

---

## 工具使用规范

### LSP 优先

永远优先使用 LSP 相关功能 来浏览和理解代码，而不是通过搜索关键词的方式。这种方式效率更高。

### MARKDOWN 修改模式

系统提供了两种文件修改（apply-diff，write-to）方式，分别是原生工具调用和直接使用markdown声明修改。

大多数情况下，你只需要使用 markdown 方式 声明你需要修改的内容，系统会自动识别、解析、执行修改。

比如：

```write_to file-path
content
```

系统监测到该格式，将会自动将文本写入。使用原生调用格式时，当需要修改、写入的内容过多时，容易超时。但是使用markdown可以流式输出。
