每次修改工具后，实际上都需要同步修改：

packages/types/src/tool.ts

和

NativeToolCallParser.ts

但是tsc完全可以检查出该问题。

如果需要自动批准，还需要额外在

src/core/auto-approval/index.ts

中也添加相关的配置。

项目应该基于显式的配置，而不是基于潜在的约定。如果两个东西有联系，则应该直接在代码上明确关联，并且当缺失时报错。
